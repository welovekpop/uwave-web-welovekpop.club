import initialState from '../theme'; // eslint-disable-next-line no-unused-vars

export default function reduce(state, action) {
  if (state === void 0) {
    state = initialState;
  }

  if (action === void 0) {
    action = {};
  }

  // Nothing for now!
  return state;
}
//# sourceMappingURL=theme.js.map
